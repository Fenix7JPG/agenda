# Framework web
fastapi>=0.115
uvicorn[standard]>=0.32

# Configuración y validación
pydantic>=2.9
python-dotenv>=1.0

# Seguridad (cookie de sesión firmada)
itsdangerous>=2.2

# Turso se accede por su API HTTP oficial (/v2/pipeline, protocolo Hrana)
# implementada con la librería estándar en app/db.py; sin dependencias extra.

# Pruebas
pytest>=8.3
httpx>=0.27

# PDF del horario (descarga de una hoja)
reportlab>=4.2
pillow>=11.0
